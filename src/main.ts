import "./style.scss";

//const slider = document.getElementById("slider");
// setInterval(() => {
//   if (slider !== null) {
//     const currentTranslateX = parseInt(slider.style.transform.split("(")[1]) || 0;
//     slider.style.transform = `translateX(${currentTranslateX - 400}px)`;
//   }
// }, 3000);

